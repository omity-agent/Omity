diff --git a/dist/cjs/index.cjs b/dist/cjs/index.cjs
index d60f3c7bd03767a5bd28ac5df68e4b83fb6ac77a..d91d9adb4caaf2fa0dcd81d2df2a0807c4ae103f 100644
--- a/dist/cjs/index.cjs
+++ b/dist/cjs/index.cjs
@@ -1159,17 +1159,16 @@ class Virtualizer {
       this._iosDeferredAdjustment += delta;
       return false;
     } else {
-      const target = this.getScrollOffset() + this.scrollAdjustments + delta;
+      const target = Math.max(0, Math.min(this.getScrollOffset() + this.scrollAdjustments + delta, this.getTotalSize() - this.getSize()));
       const el = this.scrollElement;
       const maxAtWrite = el !== null && ("scrollHeight" in el || "document" in el) ? this.getMaxScrollOffset() : null;
       this._clampedAdjustment = maxAtWrite !== null && target > maxAtWrite + 0.5 ? { target, maxAtWrite } : null;
       this._scrollToOffset(this.getScrollOffset(), {
-        adjustments: this.scrollAdjustments += delta,
+        adjustments: this.scrollAdjustments = target - this.getScrollOffset(),
         behavior
       });
       if (this.scrollOffset !== null) {
         this.scrollOffset += this.scrollAdjustments;
-        if (this.scrollOffset < 0) this.scrollOffset = 0;
         this.scrollAdjustments = 0;
       }
       return true;
diff --git a/dist/esm/index.js b/dist/esm/index.js
index e4547ec39057bdbed965d4417e242c592a194c94..cfbedd6d8a8fbb672664614b121af929bca962a6 100644
--- a/dist/esm/index.js
+++ b/dist/esm/index.js
@@ -1157,17 +1157,16 @@ class Virtualizer {
       this._iosDeferredAdjustment += delta;
       return false;
     } else {
-      const target = this.getScrollOffset() + this.scrollAdjustments + delta;
+      const target = Math.max(0, Math.min(this.getScrollOffset() + this.scrollAdjustments + delta, this.getTotalSize() - this.getSize()));
       const el = this.scrollElement;
       const maxAtWrite = el !== null && ("scrollHeight" in el || "document" in el) ? this.getMaxScrollOffset() : null;
       this._clampedAdjustment = maxAtWrite !== null && target > maxAtWrite + 0.5 ? { target, maxAtWrite } : null;
       this._scrollToOffset(this.getScrollOffset(), {
-        adjustments: this.scrollAdjustments += delta,
+        adjustments: this.scrollAdjustments = target - this.getScrollOffset(),
         behavior
       });
       if (this.scrollOffset !== null) {
         this.scrollOffset += this.scrollAdjustments;
-        if (this.scrollOffset < 0) this.scrollOffset = 0;
         this.scrollAdjustments = 0;
       }
       return true;
diff --git a/src/index.ts b/src/index.ts
index 55c276edfa77aff12e23aa47cd0db65851642bdc..f5ee91ac61237e167155efa7d8bb24f87594d52f 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -716,7 +716,15 @@ export class Virtualizer<
       this._iosDeferredAdjustment += delta
       return false
     } else {
-      const target = this.getScrollOffset() + this.scrollAdjustments + delta
+      // Use the measured extent: the DOM sizer may not have committed yet.
+      // Growth from an unscrollable list must not create an unreachable offset.
+      const target = Math.max(
+        0,
+        Math.min(
+          this.getScrollOffset() + this.scrollAdjustments + delta,
+          this.getTotalSize() - this.getSize(),
+        ),
+      )
       // Guarded so a bare test double without `scrollHeight` / `document`
       // does not crash in `getMaxScrollOffset`.
       const el = this.scrollElement
@@ -729,7 +737,7 @@ export class Virtualizer<
           ? { target, maxAtWrite }
           : null
       this._scrollToOffset(this.getScrollOffset(), {
-        adjustments: (this.scrollAdjustments += delta),
+        adjustments: (this.scrollAdjustments = target - this.getScrollOffset()),
         behavior,
       })
       // Eagerly carry the intended target in `scrollOffset` so callers that
@@ -743,13 +751,6 @@ export class Virtualizer<
       // `scrollAdjustments` to keep their sum invariant.
       if (this.scrollOffset !== null) {
         this.scrollOffset += this.scrollAdjustments
-        // Clamp only the lower bound: a negative offset is unreachable, and
-        // on an unscrollable element (content fits the viewport) no scroll
-        // event ever fires to correct it, permanently skewing
-        // getDistanceFromEnd() and wedging _flushIosDeferredIfReady (#1229).
-        // Upper-bound overflow stays untouched — it is transiently
-        // legitimate mid-prepend while the consumer's sizer catches up.
-        if (this.scrollOffset < 0) this.scrollOffset = 0
         this.scrollAdjustments = 0
       }
       return true
